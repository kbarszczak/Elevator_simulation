package app.simulation.elevator.model;

/**
 * The enum that describes either the movement of the elevator or the direction for elevator request
 */
public enum Direction {

    UP, DOWN, NOT_IN_MOVE

}
